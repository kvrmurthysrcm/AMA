New Project: Create Application for Amazon Market Place vendors to see the sales trends using analytics on historic data.
Amazon Market Place provides historic data for  the vendors/its clients.
Keep this in the context save prompts, results in a new name so a I can continue this if I leave it for a while.

1) tell me whats the format of the data Amazon market place provides for the vendors in amazon.com
2) I need to build a simple UI to show basic sales/service details. This is already built using Power BI.
    However can we build simple Angular UI + Spring Boot APIs or Python API, which ever is quick to develop and scale. I am looking at multi tenant solution so many vendors can use the service, so I am looking for Spring Boot APIs, with OAuth2 spec security enabled.
3) I am looking forward to use the MCP server to query the Amazon Market place data.
    Based on the user query --> fed it to an Agent that will decide what to do and what to query.
    Based on it, it should dynamically query the Market Place data, provide the proper response.
    Provide the response in a graphic UI like a pie chart or bar chart or the best suited based on the query.

Keep this in memory for this project as base and lets improve based on the plan you can provide.
First analyze the requirement and provide me with high level solution and ready made products available to review.

as mentioned , need the Amazon market place data schema, format first.




